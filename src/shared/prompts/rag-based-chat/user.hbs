당신이 확인할 수 있는 정보는 아래와 같습니다.

이전 대화 목록: (각각의 대화는 Q(질문)과 A(답변)으로 이뤄짐):
{{#each previousTurns}}
	- Q:
	{{this.question}}
	{{#if this.answer}}
		- A:
		{{this.answer}}
	{{else}}
		- A: (답변없음)
	{{/if}}
{{/each}}

제공된 정보 (각각의 정보는 문구와 부가정보로 이뤄짐):
{{#each contexts}}
	- Text:
	{{this.text}}
	{{#if this.metadata}}
		Metadata:
		{{#each this.metadata}}
			•
			{{@key}}
			=
			{{this}}{{#unless @last}},{{/unless}}
		{{/each}}
	{{/if}}
{{/each}}

사용자의 질문:
{{question}}
